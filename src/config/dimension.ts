export enum ICON_DIMENSION {
  FOOTER_SOCIAL_ICON = 30,
}

export enum APP_NAVBAR {
  HEIGHT = 60,
}

export const RADIUS = {
  BORDER: {
    borderRadius: 2,
  },
}

export enum APP_FIELD {
  HEIGHT = 90,
}
